extends layout 

block content 
  h1= title

  ul 
    each user in users
      li 
        a(href="/admin/user/detail/" + user._id) #{user.username}
        if (user.auth===0)
          |  unauthorized

  hr

  pre= users