extends layout 

block content 
  h1= title

  //- ul
  //- each estimate in estimates
  //-   li 
  //-     a(href="estimate/" + estimate._id) #{estimate.platform[0].name}

  each val in results
    pre= val
    p= val.count

    
